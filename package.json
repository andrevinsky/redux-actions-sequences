{
  "name": "redux-actions-sequences",
  "version": "1.0.3",
  "description": "Make sequences of actions trigger a new action",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel ./src/index.js -o ./lib/index.js",
    "prepublish": "npm run compile"
  },

  "repository": {
    "type": "git",
    "url": "git@github.com:AndrewRevinsky/redux-actions-sequences.git"
  },

  "keywords": [
    "redux",
    "actions",
    "sequences"
  ],
  "author": "AndrewRevinsky",
  "license": "ISC",
  "dependencies": {
    "flux-standard-action": "^0.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.17.0",
    "babel-preset-es2015": "^6.16.0"
  }
}
